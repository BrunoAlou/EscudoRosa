{
  "name": "meu-app-mvc",
  "version": "1.0.0",
  "description": "Aplicação de segurança para reportar incidentes e navegar por áreas de risco",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev-full": "concurrently \"npm run dev\" \"npm run live-server\"",
    "live-server": "live-server --port=5500 --entry-file=views/index.html --open=/views/index.html --mount=/public:./public --mount=/views:./views",
    "build": "echo 'Build completed'",
    "postinstall": "echo 'Dependencies installed successfully'"
  },
  "keywords": ["security", "safety", "nodejs", "express", "railway"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "connect-livereload": "^0.6.1",
    "live-server": "^1.2.2",
    "livereload": "^0.9.3",
    "nodemon": "^3.1.10"
  }
}
